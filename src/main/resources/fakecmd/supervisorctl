***REMOVED***!/usr/bin/env bash

***REMOVED*** fake uberspace commands for testing

subCat=$1           ***REMOVED*** status

if [[ "$subCat" = "status" ]]
then
    cat "$(dirname "$0")/output_supervisorctl_status.txt"
fi