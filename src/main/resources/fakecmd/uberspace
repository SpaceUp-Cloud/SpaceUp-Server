#!/bin/bash -x
trap 'echo "$BASH_COMMAND" failed with error code $?' ERR

# fake uberspace commands for testing

subCat=$1           # web
subSubCat=$2        # domain / backend
subSubSubCat=$3     # list

cat "output_$0_$1_$2_$3.txt"